import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'yemi.ionic.photogallery',
  appName: 'photo-gallery',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
